# FirstData MCP Server Configuration
# 复制此文件为 .env 并填入实际值

# ============================================================================
# Anthropic API Configuration (Required)
# ============================================================================

# Anthropic API Key (必需 - 用于 LLM Agent)
# 获取地址: https://console.anthropic.com/settings/keys
ANTHROPIC_AUTH_TOKEN=

# Anthropic API Base URL (可选 - 用于自定义 API 网关)
# 留空则使用官方地址: https://api.anthropic.com
ANTHROPIC_BASE_URL=

# ============================================================================
# Agent Model Configuration (Optional)
# ============================================================================

# LLM 模型名称 (可选)
# 默认值: gemini-3-flash-preview（推荐，速度快且成本低）
QUERY_UNDERSTANDING_MODEL=gemini-3-flash-preview

# ============================================================================
# MCP Server Authentication (Optional)
# ============================================================================

# API Key for Bearer Token Authentication (可选)
# 如果设置，客户端需要提供 "Authorization: Bearer <token>" header
# 留空则禁用认证（本地开发推荐留空）
MCP_API_KEY=

# ============================================================================
# External Services (Optional)
# ============================================================================

# 指令生成 API (可选 - 用于 get_instructions 工具)
INSTRUCTION_API_URL=https://mingjing.mininglamp.com/api/mano-plan/instruction/v1

# Web 搜索 MCP 服务 (可选 - 用于 Agent 的网络搜索)
WEB_SEARCH_MCP_URL=https://gateway.deepminer.ai/web-search/mcp
WEB_SEARCH_MCP_AUTH_TOKEN=
