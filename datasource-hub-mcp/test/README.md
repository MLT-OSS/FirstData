# DataSource Hub MCP 服务器 - 测试

## 运行测试

要验证 MCP 服务器是否正常工作，运行测试套件：

```bash
# 从 datasource-hub-mcp 目录
python test/test_server.py

# 或从 test 目录内
cd test
python test_server.py
```

## 测试检查内容

测试套件运行 4 个综合测试：

### 测试 1：青年失业率搜索（JSON 格式）
- 查询："youth unemployment China USA"
- 验证：JSON 输出格式、相关性评分、质量评分
- 预期：21+ 个来自就业统计来源的匹配项

### 测试 2：气候数据搜索（Markdown 格式）
- 查询："climate temperature global warming"
- 验证：Markdown 格式化、搜索标头生成
- 预期：40+ 个来自环境数据源的匹配项

### 测试 3：带 API 的金融市场
- 查询："stock market financial data API"
- 验证：API 过滤、综合搜索
- 预期：100+ 个匹配项，其中几个提供 API 访问

### 测试 4：无结果情况
- 查询："xyzabc123nonexistent"
- 验证：优雅处理无结果
- 预期：清晰的"无结果"消息

## 预期输出

当测试通过时，你应该看到：

```
======================================================================
DataSource Hub MCP Server - Test Suite
======================================================================

📊 Test 1: Youth Unemployment Search (JSON format)
----------------------------------------------------------------------
✓ Found 21 matches
✓ Returned 3 results
...

🌍 Test 2: Climate Data Search (Markdown format)
----------------------------------------------------------------------
✓ Generated Markdown output: 2530 characters
✓ Contains 'Search Results': Yes
...

💰 Test 3: Financial Markets with API
----------------------------------------------------------------------
✓ Found 101 matches
✓ Sources with API: 4
...

🔍 Test 4: Query with No Results
----------------------------------------------------------------------
✓ Response: No data sources found matching...

======================================================================
✅ All Tests Completed Successfully!
======================================================================
```

## 故障排除

### 导入错误

如果看到 `ModuleNotFoundError: No module named 'server'`：
- 确保从正确的目录运行
- 测试会自动将父目录添加到 sys.path

### MCP 依赖缺失

如果看到 `ModuleNotFoundError: No module named 'mcp'`：
```bash
pip install -r requirements.txt
```

### 未找到数据源

如果测试显示加载了 0 个数据源：
- 验证 `sources/` 目录存在于父目录中
- 检查 sources 子目录中是否存在 JSON 文件
- 确保文件权限允许读取

## 测试文件

- **test_server.py**：验证所有核心功能的综合测试套件
  - 测试不同查询和参数的搜索
  - 验证 JSON 和 Markdown 输出格式
  - 检查相关性评分和质量评估
  - 测试边缘情况的错误处理
